# Point a vhost at the static export. Example server name: static.revanax.local
server {
    listen 80;
    server_name static.revanax.local;
    root /home/satish/project/website/revanax/static;
    index index.html;

    location / {
        try_files $uri $uri/ $uri/index.html =404;
    }
}
